{
  "name": "sog-loader",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:core": "pnpm --filter @sog-loader/core run build",
    "version:bump": "lerna version --no-push --no-git-tag-version"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.15.1",
  "devDependencies": {
    "@lerna-lite/cli": "^4.11.5",
    "@lerna-lite/version": "^4.11.5"
  },
  "engines": {
    "pnpm": "10.x",
    "node": ">=18"
  }
}
